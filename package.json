{
  "name": "local-diary",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "pretest": "pnpm --filter @diary/contracts build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r typecheck",
    "test:e2e": "playwright test",
    "test:e2e:cleanup": "node scripts/verify-playwright-cleanup.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^26.1.1",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  }
}
