{
  "name": "@diary/desktop",
  "productName": "Local Diary",
  "version": "0.1.2",
  "description": "Private local-first diary for Windows",
  "author": "Local Diary",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "pnpm --filter @diary/server build && pnpm --filter @diary/web build && tsc -p tsconfig.build.json",
    "generate:icon": "node scripts/generate-icon.mjs",
    "verify:binaries": "node scripts/verify-binaries.mjs",
    "start": "electron .",
    "package": "pnpm generate:icon && pnpm build && electron-builder --config electron-builder.yml --dir",
    "dist": "pnpm generate:icon && pnpm build && electron-builder --config electron-builder.yml",
    "release:win": "pnpm dist --win && node scripts/release-checksums.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@diary/server": "workspace:*"
  },
  "devDependencies": {
    "electron": "43.2.0",
    "electron-builder": "26.15.3",
    "sharp": "0.35.3"
  }
}
